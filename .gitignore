# Dependency directories
node_modules/

# Local env files
.env
.env.local
.env.*.local

# Secrets locali per wrangler dev
.dev.vars
.dev.vars.*

# Local database and upload storage (hybrid mode fallbacks)
data/
uploads/
tmp/

# OS files
.DS_Store
Thumbs.db

# Stato locale di Wrangler (D1 locale, cache workerd)
.wrangler/

.venv-opt/

# Output della build (npm run build) servito da Cloudflare come static assets
dist/
